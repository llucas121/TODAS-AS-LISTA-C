// Fazer um programa para calcular uma trajetória parabólica. O programa deve:

// Receber do usuário a altura relativa ao solo de onde o projétil é lançado, 
// a velocidade inicial em m/s e o ângulo de lançamento
// Calcular e mostrar a quantos metros de distância o projétil atingirá o chão, considerando
// aceleração da gravidade igual a 9,81 m/s2.

#include <stdio.h>
#include <stdlib.h>

int main(){

  int alt, vel, ang, acelGrav, soma, result;

  printf("Qual altura relativa ao solo de onde o projétil é lançado:");
  scanf("%d", alt);
  printf("Qual a velocidade inicial em m/s:");
  scanf("%d", vel);
  printf("Qual o ângulo de lançamento:");
  scanf("%d", ang);

  

}